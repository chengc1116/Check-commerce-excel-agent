# -*- coding: utf-8 -*-
"""
产品立项审核Agent — 业务常量配置

运行时可变参数（密钥、模型、URL等）全部在 .env 中管理，本文件只放：
  1. 业务常量（评分阈值、关键词库等）
  2. 不随部署变化的默认值
"""

import os

# ============================================================
# 服务配置（可在 .env 中覆盖）
# ============================================================
HOST = os.getenv("HOST", "0.0.0.0")
PORT = int(os.getenv("PORT", "8000"))
DEBUG = os.getenv("DEBUG", "true").lower() == "true"
UPLOAD_DIR = os.getenv("UPLOAD_DIR", "./uploads")
MAX_FILE_SIZE = 50 * 1024 * 1024  # 50MB
ALLOWED_EXTENSIONS = {".docx", ".doc", ".xlsx", ".xls"}

# ============================================================
# 评估配置（业务常量）
# ============================================================
MIN_CONFIDENCE_SCORE = 0.6  # 最低置信度阈值
PROPORTION_SUM_TOLERANCE = 0.05  # 占比总和容差（±5%）

# ============================================================
# 产品数据库（可在 .env 中覆盖路径）
# ============================================================
PRODUCT_DB_PATH = os.getenv("PRODUCT_DB_PATH", "./data/project_review.db")
